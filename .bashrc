#
# ~/.bashrc
#

TERMINAL="alacritty"
BROWSER="firefox"
EDITOR="helix"

# If not running interactively, don't do anything


[[ $- != *i* ]] && return

alias hx="helix"
alias dots="cd ~/repos/dotfiles/"

# PS1='[\u@\h \W]\$ '
#
